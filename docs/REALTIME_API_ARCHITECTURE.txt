================================================================================
OpenAI Realtime API Integration - Architecture Diagram
================================================================================
Swarm ID: swarm-1762909553486-yd1fw3wqi
Date: 2025-11-11

================================================================================
SYSTEM OVERVIEW
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│                        OpenAI Realtime API                              │
│            wss://api.openai.com/v1/realtime?model=...                   │
│                                                                           │
│  Features:                                                               │
│  • Persistent WebSocket connection                                      │
│  • Real-time audio streaming (input + output)                          │
│  • Server-side VAD (Voice Activity Detection)                          │
│  • Function calling for robot actions                                   │
│  • Multi-modal responses (text + audio)                                │
│  • Conversation context management                                      │
└─────────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │ WebSocket
                                    │ (WSS)
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│              RealtimeConnectionManager (Shared Component)               │
│                                                                           │
│  Responsibilities:                                                       │
│  ├─ Establish/maintain WebSocket connection                            │
│  ├─ Auto-reconnection with exponential backoff                         │
│  ├─ Event routing to registered handlers                               │
│  ├─ Send events from nodes to API                                      │
│  └─ Connection statistics and monitoring                               │
│                                                                           │
│  Event Handlers Registry:                                               │
│  ├─ input_audio_buffer.speech_started                                  │
│  ├─ conversation.item.input_audio_transcription.completed              │
│  ├─ response.created                                                    │
│  ├─ response.function_call_arguments.done                              │
│  ├─ response.audio.delta                                               │
│  └─ ... (20+ event types)                                              │
└─────────────────────────────────────────────────────────────────────────┘
          │                         │                         │
          │                         │                         │
          ▼                         ▼                         ▼
┌───────────────────┐    ┌───────────────────┐    ┌───────────────────┐
│ Speech Recog 22   │    │    AI Node 22     │    │ Speech Synth 22   │
│                   │    │                   │    │                   │
│ Audio Input       │    │ Conversation Mgmt │    │ Audio Output      │
│ Streaming         │    │ Function Calling  │    │ Playback          │
└───────────────────┘    └───────────────────┘    └───────────────────┘
          │                         │                         │
          │                         │                         │
          └─────────────────────────┼─────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   Nevil Message Bus       │
                    │   (Inter-Node Messaging)  │
                    └───────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   Other Nevil Nodes       │
                    │   • Navigation            │
                    │   • Visual                │
                    │   • LED Indicators        │
                    └───────────────────────────┘

================================================================================
MESSAGE FLOW: Voice Input → Response Cycle
================================================================================

┌──────────────┐
│   USER       │ "Move forward"
└──────┬───────┘
       │ 🎤 Speech
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 1: AUDIO CAPTURE                                                   │
│ speech_recognition_node22                                                │
│ ├─ PyAudio captures audio from microphone (24kHz PCM16)                │
│ ├─ Audio chunks → base64 encode → stream to Realtime API               │
│ └─ WebSocket send: input_audio_buffer.append                           │
└──────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 2: SERVER-SIDE PROCESSING (OpenAI)                                 │
│ ├─ VAD detects speech start → Event: input_audio_buffer.speech_started │
│ ├─ Whisper transcribes audio → "Move forward"                          │
│ ├─ Event: conversation.item.input_audio_transcription.completed        │
│ └─ GPT-4 Realtime processes request + generates response               │
└──────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 3: TRANSCRIPTION HANDLING                                           │
│ speech_recognition_node22                                                │
│ ├─ Receive event: conversation.item.input_audio_transcription.completed│
│ ├─ Extract transcript: "Move forward"                                  │
│ └─ Publish to message bus: voice_command                               │
│    {                                                                     │
│      "text": "Move forward",                                            │
│      "confidence": 0.95,                                                │
│      "timestamp": ...,                                                  │
│      "source": "realtime_api"                                           │
│    }                                                                     │
└──────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 4: AI PROCESSING & FUNCTION CALLING                                │
│ ai_node22                                                                │
│ ├─ Receive event: response.created                                     │
│ ├─ Publish to message bus: system_mode = "thinking"                    │
│ ├─ GPT decides to call function: move_forward                          │
│ ├─ Receive event: response.function_call_arguments.done                │
│ │   {                                                                   │
│ │     "name": "move_forward",                                          │
│ │     "arguments": {"distance": 30, "speed": 50}                       │
│ │   }                                                                   │
│ ├─ Execute handler: _handle_move_forward(args)                         │
│ ├─ Publish to message bus: robot_action                                │
│ │   {                                                                   │
│ │     "actions": ["forward"],                                          │
│ │     "distance": 30,                                                  │
│ │     "speed": 50,                                                     │
│ │     "priority": 100                                                  │
│ │   }                                                                   │
│ └─ Send function result back to API                                    │
└──────────────────────────────────────────────────────────────────────────┘
       │
       │ (Navigation node executes movement)
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 5: AI GENERATES SPEECH RESPONSE                                    │
│ OpenAI Realtime API                                                      │
│ ├─ GPT generates text response: "Moving forward now"                   │
│ ├─ TTS synthesizes audio (voice: alloy)                                │
│ └─ Streams audio chunks via WebSocket                                  │
│    Event: response.audio.delta (repeated for each chunk)               │
└──────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 6: AUDIO PLAYBACK                                                  │
│ speech_synthesis_node22                                                  │
│ ├─ Receive event: response.audio.delta                                 │
│ ├─ Decode base64 audio chunk → raw PCM16 bytes                         │
│ ├─ Add to playback buffer (queue)                                      │
│ ├─ Playback thread: buffer → PyAudio → speakers                        │
│ ├─ Publish to message bus: speaking_status                             │
│ │   {                                                                   │
│ │     "speaking": true,                                                │
│ │     "text": "Moving forward now",                                    │
│ │     "timestamp": ...                                                 │
│ │   }                                                                   │
│ └─ Receive event: response.audio.done → playback complete              │
└──────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│  SPEAKERS    │ "Moving forward now" 🔊
└──────────────┘

Total Latency: ~800-2300ms (vs 5-13s for discrete nodes)

================================================================================
NODE DETAIL: speech_recognition_node22
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  speech_recognition_node22                                              │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  PyAudio Microphone Capture (24kHz PCM16)                  │       │
│  │  ├─ Continuous audio callback                              │       │
│  │  ├─ 4800 samples per chunk (200ms)                         │       │
│  │  └─ Stream to AudioStreamer                                │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  AudioStreamer                                              │       │
│  │  ├─ Base64 encode audio chunks                             │       │
│  │  └─ Send to RealtimeConnectionManager                      │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Event Handlers                                             │       │
│  │  ├─ input_audio_buffer.speech_started                      │       │
│  │  │   → Publish: speech_detected = true                     │       │
│  │  │                                                          │       │
│  │  ├─ input_audio_buffer.speech_stopped                      │       │
│  │  │   → Publish: speech_detected = false                    │       │
│  │  │                                                          │       │
│  │  └─ conversation.item.input_audio_transcription.completed  │       │
│  │      → Extract transcript                                  │       │
│  │      → Publish: voice_command                              │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Message Bus Publishers                                     │       │
│  │  ├─ voice_command (transcribed speech)                     │       │
│  │  ├─ speech_detected (VAD events)                           │       │
│  │  └─ listening_status (streaming state)                     │       │
│  └─────────────────────────────────────────────────────────────┘       │
│                                                                           │
│  State Management:                                                       │
│  • is_streaming: bool                                                   │
│  • Pause/resume based on speaking_status                               │
│                                                                           │
│  Configuration:                                                          │
│  • sample_rate: 24000                                                   │
│  • chunk_size: 4800                                                     │
│  • enable_vad: true                                                     │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
NODE DETAIL: ai_node22
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  ai_node22                                                              │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Session Configuration                                      │       │
│  │  ├─ System prompt (Nevil personality)                      │       │
│  │  ├─ Voice: alloy                                            │       │
│  │  ├─ Modalities: ["text", "audio"]                          │       │
│  │  ├─ Turn detection: server_vad                             │       │
│  │  └─ Tools: [move_forward, turn, take_snapshot, ...]        │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Function Registry                                          │       │
│  │  ├─ move_forward(distance, speed)                          │       │
│  │  │   → Publish: robot_action                               │       │
│  │  │                                                          │       │
│  │  ├─ turn(direction, angle)                                 │       │
│  │  │   → Publish: robot_action                               │       │
│  │  │                                                          │       │
│  │  ├─ take_snapshot()                                        │       │
│  │  │   → Publish: snap_pic                                   │       │
│  │  │                                                          │       │
│  │  └─ play_sound(sound_name)                                 │       │
│  │      → Publish: sound_effect                               │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Event Handlers                                             │       │
│  │  ├─ response.created                                        │       │
│  │  │   → Publish: system_mode = "thinking"                   │       │
│  │  │                                                          │       │
│  │  ├─ response.function_call_arguments.done                  │       │
│  │  │   → Execute function handler                            │       │
│  │  │   → Send function output back to API                    │       │
│  │  │                                                          │       │
│  │  ├─ response.done                                           │       │
│  │  │   → Extract text response                               │       │
│  │  │   → Publish: text_response                              │       │
│  │  │                                                          │       │
│  │  └─ conversation.item.created                              │       │
│  │      → Store in conversation history                       │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Message Bus Publishers                                     │       │
│  │  ├─ text_response (AI text)                                │       │
│  │  ├─ robot_action (function calls → commands)               │       │
│  │  ├─ snap_pic (camera requests)                             │       │
│  │  ├─ sound_effect (sound playback)                          │       │
│  │  └─ system_mode (thinking/error/idle)                      │       │
│  └─────────────────────────────────────────────────────────────┘       │
│                                                                           │
│  State Management:                                                       │
│  • conversation_items: list (history)                                  │
│  • current_response_id: str                                             │
│                                                                           │
│  Configuration:                                                          │
│  • temperature: 0.7                                                     │
│  • max_tokens: 4096                                                     │
│  • max_conversation_items: 50                                           │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
NODE DETAIL: speech_synthesis_node22
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  speech_synthesis_node22                                                │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Event Handlers                                             │       │
│  │  ├─ response.audio.delta                                    │       │
│  │  │   → Decode base64 audio chunk                            │       │
│  │  │   → Add to playback buffer (queue)                       │       │
│  │  │                                                          │       │
│  │  ├─ response.audio.done                                     │       │
│  │  │   → Mark audio complete                                 │       │
│  │  │   → Wait for buffer drain                               │       │
│  │  │                                                          │       │
│  │  ├─ response.audio_transcript.delta                        │       │
│  │  │   → Publish partial transcript                          │       │
│  │  │                                                          │       │
│  │  └─ response.audio_transcript.done                         │       │
│  │      → Publish complete transcript                         │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Audio Buffer (Queue)                                       │       │
│  │  ├─ Max size: 100 chunks                                    │       │
│  │  ├─ Start threshold: 5 chunks                               │       │
│  │  └─ FIFO ordering                                           │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Playback Thread                                            │       │
│  │  ├─ Continuously reads from buffer                          │       │
│  │  ├─ Writes to PyAudio output stream                         │       │
│  │  └─ Handles buffer underrun recovery                        │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  PyAudio Speaker Output (24kHz PCM16)                       │       │
│  │  ├─ Real-time playback                                      │       │
│  │  └─ Concurrent with audio chunk receiving                   │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Message Bus Publishers                                     │       │
│  │  └─ speaking_status                                         │       │
│  │      ├─ speaking: true/false                                │       │
│  │      ├─ text: transcript                                    │       │
│  │      └─ partial: true/false                                 │       │
│  └─────────────────────────────────────────────────────────────┘       │
│                                                                           │
│  State Management:                                                       │
│  • is_speaking: bool                                                    │
│  • current_response_id: str                                             │
│  • audio_chunks_played: int                                             │
│                                                                           │
│  Configuration:                                                          │
│  • sample_rate: 24000                                                   │
│  • buffer_size: 100                                                     │
│  • start_threshold: 5                                                   │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
REALTIME CONNECTION MANAGER DETAIL
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  RealtimeConnectionManager                                              │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  WebSocket Connection                                       │       │
│  │  ├─ URL: wss://api.openai.com/v1/realtime?model=...        │       │
│  │  ├─ Headers:                                                │       │
│  │  │   - Authorization: Bearer {api_key}                     │       │
│  │  │   - OpenAI-Beta: realtime=v1                            │       │
│  │  ├─ Max message size: 10MB                                 │       │
│  │  └─ Connection state: connected/disconnected               │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Event Loop (Asyncio)                                       │       │
│  │  ├─ Runs in dedicated thread                               │       │
│  │  ├─ Receives messages from WebSocket                       │       │
│  │  ├─ Sends messages to WebSocket                            │       │
│  │  └─ Manages reconnection logic                             │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Event Handlers Registry                                    │       │
│  │  {                                                           │       │
│  │    'event_type_1': [handler_1a, handler_1b],               │       │
│  │    'event_type_2': [handler_2a],                           │       │
│  │    ...                                                      │       │
│  │  }                                                           │       │
│  │                                                              │       │
│  │  Registration API:                                          │       │
│  │  • register_event_handler(type, handler)                   │       │
│  │  • unregister_event_handler(type, handler)                 │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Reconnection Logic                                         │       │
│  │  ├─ Initial delay: 1s                                       │       │
│  │  ├─ Exponential backoff: 2x per attempt                    │       │
│  │  ├─ Max delay: 30s                                          │       │
│  │  └─ Unlimited attempts (while enabled)                     │       │
│  │                                                              │       │
│  │  States:                                                     │       │
│  │  ├─ Connected → receive events                             │       │
│  │  ├─ Disconnected → schedule reconnect                      │       │
│  │  └─ Shutdown → stop reconnection                           │       │
│  └─────────────┬───────────────────────────────────────────────┘       │
│                │                                                         │
│                ▼                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │  Statistics                                                 │       │
│  │  ├─ events_sent: int                                       │       │
│  │  ├─ events_received: int                                   │       │
│  │  ├─ errors: int                                            │       │
│  │  └─ reconnect_attempts: int                                │       │
│  └─────────────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
PERFORMANCE COMPARISON
================================================================================

┌────────────────────────────────────────────────────────────────────────┐
│                    Discrete Nodes vs Realtime Nodes                    │
└────────────────────────────────────────────────────────────────────────┘

Latency Breakdown:

DISCRETE NODES (Traditional Pipeline):
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ Record Audio    │ →   │ STT (Whisper)   │ →   │ GPT Processing  │
│ 1-2s            │     │ 2-3s            │     │ 1-2s            │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                                                          ↓
                                                  ┌─────────────────┐
                                                  │ TTS Generation  │
                                                  │ 2-3s            │
                                                  └─────────────────┘
                                                          ↓
                                                  ┌─────────────────┐
                                                  │ Audio Playback  │
                                                  │ 1-2s            │
                                                  └─────────────────┘
TOTAL: 5-13 seconds

REALTIME NODES (Streaming Pipeline):
┌─────────────────────────────────────────────────────────────────────┐
│ Stream Audio → Realtime STT → GPT → Stream Audio Out → Play        │
│ 200ms           300ms          500ms   100ms            100ms       │
└─────────────────────────────────────────────────────────────────────┘
TOTAL: 800-2300 milliseconds

IMPROVEMENT: 6-10x FASTER

Resource Usage:

┌─────────────┬───────────────┬───────────────┬────────────┐
│ Resource    │ Discrete      │ Realtime      │ Change     │
├─────────────┼───────────────┼───────────────┼────────────┤
│ CPU (avg)   │ 15-25%        │ 10-15%        │ -33% to    │
│             │               │               │ -50%       │
├─────────────┼───────────────┼───────────────┼────────────┤
│ Memory      │ 150MB         │ 200MB         │ +33%       │
├─────────────┼───────────────┼───────────────┼────────────┤
│ Bandwidth   │ ~10 KB/s      │ ~100 KB/s     │ +10x       │
├─────────────┼───────────────┼───────────────┼────────────┤
│ Disk I/O    │ High          │ Minimal       │ -90%       │
│             │ (temp files)  │ (streaming)   │            │
└─────────────┴───────────────┴───────────────┴────────────┘

================================================================================
END OF ARCHITECTURE DIAGRAM
================================================================================
